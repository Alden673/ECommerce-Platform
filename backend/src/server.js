import express from"express";import mongoose from"mongoose";import cors from"cors";import helmet from"helmet";import morgan from"morgan";import rateLimit from"express-rate-limit";import dotenv from"dotenv";import auth from"./routes/auth.js";import users from"./routes/users.js";import products from"./routes/products.js";import cart from"./routes/cart.js";import orders from"./routes/orders.js";import admin from"./routes/admin.js";import catalog from"./routes/catalog.js";dotenv.config();const app=express(),PORT=process.env.PORT||5000;app.use(helmet());app.use(cors({origin:process.env.CLIENT_URL?.split(",").map(x=>x.trim())||"*",credentials:true}));app.use(express.json({limit:"1mb"}));app.use(morgan("dev"));app.use("/api/auth",rateLimit({windowMs:15*60*1000,limit:100}));app.get("/api/health",(q,s)=>s.json({ok:true,time:new Date().toISOString()}));app.use("/api/auth",auth);app.use("/api/users",users);app.use("/api/products",products);app.use("/api/cart",cart);app.use("/api/orders",orders);app.use("/api/admin",admin);app.use("/api/catalog",catalog);app.use((q,s)=>s.status(404).json({message:"Route not found"}));app.use((e,q,s,n)=>{console.error(e);s.status(e.status||500).json({message:e.message||"Server error"})});async function start(){if(!process.env.MONGODB_URI||!process.env.JWT_SECRET)throw Error("MONGODB_URI and JWT_SECRET are required");await mongoose.connect(process.env.MONGODB_URI);app.listen(PORT,()=>console.log(`API running on ${PORT}`))}start().catch(e=>{console.error(e);process.exit(1)});
